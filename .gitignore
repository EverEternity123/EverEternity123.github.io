# ---------------------------------------------------------------------------
# 这个仓库 = 你的博客网站（GitHub Pages 从根目录发布）
# 下面这些是开发用的东西，不发布
# ---------------------------------------------------------------------------

# 本地工具（预览服务、自检脚本、截图脚本）
.tools/

# 本地助手的工作目录：里面有私人笔记，绝对不要提交
.workbuddy-ai/

# 本地预览截图
_preview/

# GitHub 令牌（用于 API 推送 / 手机编辑）。绝对不要提交！
.gh-token
.env
.env.local

# 系统文件
.DS_Store
Thumbs.db

# 依赖（本项目零依赖，正常不会有）
node_modules/

# ---------------------------------------------------------------------------
# 注意：下面这些是网站内容，必须提交
#   *.html  assets/  data/posts.json  write.config.js
# write.config.js 里只有你的用户名和仓库名，不是密钥，可以公开
# ---------------------------------------------------------------------------
